{
  "name": "rein-cli",
  "version": "0.2.0",
  "description": "Rein — keep a coding-agent run on a short rein: risk-aware routing, an invocation ceiling, inspectable file evidence, and hash-frozen replay. Codex CLI only. (formerly Relay10 / DisciplinedRun)",
  "type": "module",
  "bin": {
    "rein": "./src/cli.mjs"
  },
  "scripts": {
    "check": "npm run test && npm run lint && npm run validate:skills",
    "test": "node --test test/*.test.mjs",
    "lint": "node --check src/*.mjs && node --check src/providers/*.mjs && node --check test/*.mjs && node --check scripts/*.mjs",
    "smoke": "node src/cli.mjs doctor && node src/cli.mjs route \"research and build a small CLI\" --json",
    "validate:skills": "node scripts/validate-skill-pack.mjs",
    "verify:package": "node scripts/verify-packed-package.mjs",
    "report:launch": "node scripts/build-launch-report.mjs",
    "audit:launch": "node scripts/audit-launch-report.mjs",
    "verify:launch": "node scripts/verify-launch.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "rein",
    "relay10",
    "codex",
    "claude-code",
    "grok",
    "agent-skills",
    "agent-harness",
    "model-routing",
    "invocation-ceiling",
    "evidence-led",
    "frozen-replay",
    "clarity-eval"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/minwoo19930301/rein.git"
  },
  "bugs": {
    "url": "https://github.com/minwoo19930301/rein/issues"
  },
  "homepage": "https://github.com/minwoo19930301/rein#readme",
  "files": [
    "src",
    "schema",
    "examples",
    "test",
    "scripts",
    "docs",
    "plugins/rein",
    ".claude-plugin",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "CHANGELOG.md"
  ]
}
