node_modules/
.rein/
# Pre-0.2.0 working directories. Nothing in the current tree writes to these;
# they are ignored so a checkout that predates the Rein rename stays clean.
.relay10/
/rein.config.json
/relay10.config.json
coverage/
.DS_Store
work/
outputs/
*.log
