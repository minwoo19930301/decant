node_modules/
.decant/
# Pre-0.2.0 working directories, kept ignored so a checkout that predates the
# Decant rename stays clean. Nothing in the current tree writes to these.
.rein/
.relay10/
/decant.config.json
/rein.config.json
/relay10.config.json
coverage/
.DS_Store
work/
outputs/
*.log
